# =============================================================================
# MILERN / MYLERN -- lista de contenedores del ASI
#   contenedor 01  backend      servidor backend & api mylern
#   contenedor 02  n8n          workflow n8n mylern
#   contenedor 03  postgres     base de datos postgres mylern
#   contenedor 04  webapp       web app mylern
#   contenedor 05  proxy        reverse proxy tls
#
# Uso:  docker compose --env-file .env up -d --build
# El contexto de build es la raiz del repositorio (..), por eso este archivo se
# invoca desde deploy/ y las rutas COPY llevan el prefijo del subproyecto.
# =============================================================================

name: milern

x-logging: &logging
  driver: json-file
  options: { max-size: "10m", max-file: "3" }

services:
  # --- contenedor 03 ---------------------------------------------------------
  postgres:
    image: postgres:16-alpine
    container_name: milern-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${PGUSER}
      POSTGRES_PASSWORD: ${PGPASSWORD}
      POSTGRES_DB: ${PGDATABASE}
      # El indice global y el cron dominical se calculan en UTC.
      TZ: UTC
      PGTZ: UTC
    volumes:
      - datos_postgres:/var/lib/postgresql/data
      # Se ejecutan en orden lexicografico en el primer arranque del volumen.
      - ./postgres/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${PGUSER} -d ${PGDATABASE}"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s
    networks: [interna]
    logging: *logging

  # --- contenedor 01 ---------------------------------------------------------
  backend:
    build:
      context: ..
      dockerfile: deploy/backend/Dockerfile
    image: milern/backend:1.0.0
    container_name: milern-backend
    restart: unless-stopped
    depends_on:
      postgres: { condition: service_healthy }
    environment:
      NODE_ENV: production
      TZ: UTC
      PORT: "3000"
      LOG_LEVEL: ${LOG_LEVEL:-info}
      PGHOST: postgres
      PGPORT: "5432"
      PGUSER: ${PGUSER}
      PGPASSWORD: ${PGPASSWORD}
      PGDATABASE: ${PGDATABASE}
      JWT_SECRET: ${JWT_SECRET}
      JWT_TTL: ${JWT_TTL:-12h}
      INTERNAL_API_SECRET: ${INTERNAL_API_SECRET}
      CORS_ORIGEN: ${CORS_ORIGEN:-https://${DOMINIO}}
      DESPACHO_MAX_POR_VENTANA: ${DESPACHO_MAX_POR_VENTANA:-10}
      DESPACHO_ESPACIADO_SEG: ${DESPACHO_ESPACIADO_SEG:-60}
      DESPACHO_MAX_INTENTOS: ${DESPACHO_MAX_INTENTOS:-3}
      EVALUACION_MAX_ITEMS: ${EVALUACION_MAX_ITEMS:-20}
      # [feature 1.3] horas de silencio
      ZONA_HORARIA: ${ZONA_HORARIA:-UTC}
      SILENCIO_ACTIVO: ${SILENCIO_ACTIVO:-true}
      SILENCIO_HORA_INICIO: ${SILENCIO_HORA_INICIO:-22}
      SILENCIO_HORA_FIN: ${SILENCIO_HORA_FIN:-7}
      SILENCIO_DESPLAZAMIENTO_UE: ${SILENCIO_DESPLAZAMIENTO_UE:-54}
      RATE_LIMIT_ACTIVO: ${RATE_LIMIT_ACTIVO:-false}
      BOOTSTRAP_EMAIL: ${BOOTSTRAP_EMAIL:-}
      BOOTSTRAP_PASSWORD: ${BOOTSTRAP_PASSWORD:-}
      BOOTSTRAP_TELEGRAM_CHAT_ID: ${BOOTSTRAP_TELEGRAM_CHAT_ID:-}
    networks: [interna, entrada]
    logging: *logging
    # Publicado para que un proxy externo (Nginx Proxy Manager) pueda enrutar.
    # El formato es "ip:puerto". Dejar la variable vacia NO desactiva la
    # publicacion: cae al valor por defecto de abajo. Para no publicar nada
    # (caso del contenedor 05 propio) usa docker-compose.proxy-propio.yml.
    ports:
      - "${PUERTO_API:-127.0.0.1:3000}:3000"

  # --- contenedor 02 ---------------------------------------------------------
  n8n:
    image: n8nio/n8n:1.70.1
    container_name: milern-n8n
    restart: unless-stopped
    depends_on:
      postgres: { condition: service_healthy }
      backend:  { condition: service_started }
    environment:
      # El cron dominical debe dispararse a las 00:00 UTC exactas.
      GENERIC_TIMEZONE: UTC
      TZ: UTC
      # Dominio propio de n8n. Si lo sirves en un subdominio aparte
      # (n8n.midominio.com), definelo en DOMINIO_N8N; si no, cae al principal.
      # De aqui salen la URL del editor y, sobre todo, la URL base de los
      # webhooks: si esta mal, Telegram entrega los mensajes a otro sitio.
      N8N_HOST: ${DOMINIO_N8N:-${DOMINIO}}
      N8N_PORT: "5678"
      N8N_PROTOCOL: https
      WEBHOOK_URL: https://${DOMINIO_N8N:-${DOMINIO}}/
      N8N_EDITOR_BASE_URL: https://${DOMINIO_N8N:-${DOMINIO}}/
      N8N_ENCRYPTION_KEY: ${N8N_ENCRYPTION_KEY}
      # n8n 1.x NO tiene autenticacion basica: N8N_BASIC_AUTH_* se ignora en
      # silencio. La proteccion es la cuenta de propietario, que se crea en el
      # PRIMER acceso al editor. Quien entre primero se queda con la instancia,
      # y n8n guarda el token del bot de Telegram: crea esa cuenta nada mas
      # levantar el sistema, antes de publicar el subdominio.
      # Los workflows leen MILERN_API_BASE y MILERN_INTERNAL_SECRET via $env.
      N8N_BLOCK_ENV_ACCESS_IN_NODE: "false"
      MILERN_API_BASE: http://backend:3000
      MILERN_INTERNAL_SECRET: ${INTERNAL_API_SECRET}
      # Persistencia de n8n en la misma instancia de Postgres, base separada.
      DB_TYPE: postgresdb
      DB_POSTGRESDB_HOST: postgres
      DB_POSTGRESDB_PORT: "5432"
      DB_POSTGRESDB_DATABASE: ${PGDATABASE}
      DB_POSTGRESDB_SCHEMA: n8n
      DB_POSTGRESDB_USER: ${PGUSER}
      DB_POSTGRESDB_PASSWORD: ${PGPASSWORD}
      N8N_DIAGNOSTICS_ENABLED: "false"
      N8N_RUNNERS_ENABLED: "true"
      EXECUTIONS_DATA_PRUNE: "true"
      EXECUTIONS_DATA_MAX_AGE: "336"
    volumes:
      - datos_n8n:/home/node/.n8n
      # Definiciones versionadas de los workflows, para importarlas.
      - ../space_repetition:/workflows:ro
    networks: [interna, entrada]
    ports:
      - "${PUERTO_N8N:-127.0.0.1:5678}:5678"
    logging: *logging

  # --- contenedor 04 ---------------------------------------------------------
  webapp:
    build:
      context: ..
      dockerfile: deploy/webapp/Dockerfile
      args:
        # Rutas relativas: el proxy sirve SPA y API bajo el mismo origen.
        VITE_API_BASE: ""
    image: milern/webapp:1.0.0
    container_name: milern-webapp
    restart: unless-stopped
    networks: [interna, entrada]
    logging: *logging
    ports:
      - "${PUERTO_WEBAPP:-127.0.0.1:8080}:80"

  # --- contenedor 05 ---------------------------------------------------------
  # Solo hace falta si NO usas un proxy externo. Con Nginx Proxy Manager +
  # Cloudflare este contenedor sobra: levantalo con --profile proxy-propio.
  proxy:
    profiles: ["proxy-propio"]
    build:
      context: ..
      dockerfile: deploy/nginx/Dockerfile
    image: milern/proxy:1.0.0
    container_name: milern-proxy
    restart: unless-stopped
    depends_on: [backend, webapp, n8n]
    environment:
      DOMINIO: ${DOMINIO}
      # Restaura la IP real del visitante desde CF-Connecting-IP. Actívalo SOLO
      # si el trafico entra por Cloudflare: sin proxy conocido delante, confiar
      # en esa cabecera permitiria falsificar la IP y esquivar el limitador.
      CONFIAR_EN_CLOUDFLARE: ${CONFIAR_EN_CLOUDFLARE:-false}
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/certs:/etc/nginx/certs
      - certbot_webroot:/var/www/certbot
    networks: [interna, externa]
    logging: *logging

  # --- emision y renovacion del certificado (no es un contenedor del ASI) ----
  # Se ejecuta de forma puntual desde scripts/certificado.sh; comparte con el
  # proxy el webroot del reto ACME y el directorio de certificados.
  certbot:
    image: certbot/certbot:latest
    profiles: ["proxy-propio"]
    volumes:
      - certbot_conf:/etc/letsencrypt
      - certbot_webroot:/var/www/certbot
      - ./nginx/certs:/salida
    networks: [externa]

  # --- herramienta puntual (no es un contenedor del ASI) ---------------------
  cli:
    build:
      context: ..
      dockerfile: deploy/cli/Dockerfile
    image: milern/cli:1.0.0
    profiles: ["herramientas"]
    environment:
      MYLERN_URL: http://backend:3000
      MYLERN_TOKEN: ${MYLERN_CLI_TOKEN:-}
    networks: [interna]

volumes:
  datos_postgres:
  datos_n8n:
  certbot_webroot:
  certbot_conf:

networks:
  # Red donde se hablan backend, n8n, webapp y Postgres.
  interna:
    driver: bridge
  # Red por la que entra el trafico. Si usas Nginx Proxy Manager en Docker,
  # conectalo a esta red (ver guia_despligue.md) y podras enrutar por nombre de
  # contenedor sin publicar ningun puerto al host.
  entrada:
    name: ${RED_ENTRADA:-milern_entrada}
    driver: bridge
  externa:
    driver: bridge
